import { StyledLoaderIcon, Wrapper } from "./styled";

export const Loader = () => {
    return (
        <Wrapper>
            <StyledLoaderIcon />
        </Wrapper>
    );
};

export default Loader;